import React from "react";
import { ErrorMessage } from "../components/index";

export const Error = () => {
  return (
    <ErrorMessage message="We can't seem to find the page you're looking for" />
  );
};
